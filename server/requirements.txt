fastapi>=0.110
uvicorn>=0.27
pydantic>=2
numpy>=1.26
pillow>=10
opencv-python-headless>=4.9

# opencv-python-headless is required for projector PnP. Torch / DA3 / MoGe stay optional.
#   pip install torch
#   pip install git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
#   pip install git+https://github.com/microsoft/MoGe.git
# Then: LUMEN_RUN_DA3=1 LUMEN_RUN_MOGE=1 uvicorn app:app --port 8787
